#!/usr/bin/env node

/* String delimiters
    'single quotes'
    "double quotes"
    `backticks`
*/

const greet = 'Hello';
const firstName = 'Rony';

console.log(greet + ' ' + firstName + '!');
console.log(`${greet} ${firstName}!`);
