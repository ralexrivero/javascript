#!/usr/bin/env node

/**
 * without keyword
 * literals
 * var
 * let
 * const
 */

// without keywords
x = 0;
console.log(`${x} is typeof: ${typeof x}`);

// literals
1;
10;
'Charles';

// var
// avoid using

// let
// can be reassigned and assigned later

// const
// can't be modified and must be declared on initialization

// variable names

const camelCase = 'This is a cammel case';
console.log(camelCase);
